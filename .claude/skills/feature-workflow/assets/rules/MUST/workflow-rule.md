# 워크플로우 실행 규칙

> 적용 대상: 전체 워크플로우 (Step 1-5)

---

## 1. 핵심 원칙

### Context Isolation

- 각 Step은 **독립적 컨텍스트**에서 실행 권장
- Step 간 정보 전달은 **문서 파일**을 통해서만 수행
- 이전 Step의 결과물을 직접 참조하지 않고 출력 파일을 읽어서 사용

### Human in the Loop

- **중요 결정 전 사용자 승인** 필수
- Step 시작/완료 시 사용자에게 상태 안내
- 예상치 못한 상황 발생 시 사용자에게 선택지 제시

### Document as Interface

- Step 간 통신은 **`.md` 문서**로 수행
- 입력/출력 파일 형식은 템플릿을 따름
- 구조화된 형식으로 정보 전달

### Git as History

- 각 **Step 완료 시 Git 커밋** 생성
- 커밋 메시지 형식: `[TASK_ID] Step N: 간략한 설명`
- 롤백 가능한 체크포인트 유지

---

## 2. Step 실행 규칙

### 순서 준수

```
Step 1 → Step 2 → Step 3 → Step 4 → Step 5
```

- **순서를 건너뛰지 않음** (긴급 시 사용자 명시적 승인 필요)
- 이전 Step의 `status`가 `completed`여야 다음 Step 진행 가능

### Step 시작 전 필수 확인

1. **status.yaml 읽기**

   ```yaml
   current_step: step-N
   steps:
     step-N:
       status: pending | in_progress | completed
   ```

2. **입력 파일 존재 확인**
   | Step | 필수 입력 파일 |
   |------|---------------|
   | 1 | `00-user-prompt.md` |
   | 2 | `10-output-plan.md` |
   | 3 | `10-output-plan.md`, `20-output-system-design.md` |
   | 4 | `20-output-system-design.md`, `30-output-task.md`, `todos/*.md` |
   | 5 | `40-output-implementation.md` |

3. **입력 파일 내용 완전히 읽기**
   - 파일을 끝까지 읽고 내용 이해
   - 누락된 정보 있으면 이전 Step 출력 참조 또는 사용자 질문

### Step 완료 조건

1. **출력 파일 생성**
   - 템플릿 형식 준수
   - 필수 섹션 모두 포함

2. **status.yaml 업데이트**

   ```yaml
   current_step: step-N+1 # 다음 Step으로 이동
   steps:
     step-N:
       status: completed
       completed_at: '2024-01-15T10:30:00Z'
   ```

3. **Git 커밋 생성**
   ```bash
   git add .ai/tasks/<TASK_ID>/
   git commit -m "[TASK_ID] Step N: <간략한 설명>"
   ```

---

## 3. 입출력 파일 규칙

### 파일 네이밍

```
<번호>-<용도>.md
```

- `00-user-prompt.md` - 사용자 입력
- `10-output-plan.md` - Step 1 출력
- `20-output-system-design.md` - Step 2 출력
- `30-output-task.md` - Step 3 출력
- `40-output-implementation.md` - Step 4 출력
- `50-output-review.md` - Step 5 출력

### 템플릿 준수

- `assets/templates/` 디렉토리의 템플릿 참조
- 필수 섹션 누락 금지
- 추가 섹션은 허용

### 파일 수정 금지

- **이전 Step의 출력 파일을 수정하지 않음**
- 수정이 필요하면 해당 Step을 다시 실행
- 예외: `status.yaml`만 현재 Step에서 업데이트

---

## 4. 상태 관리 규칙

### status.yaml 구조

```yaml
task_id: TASK-001
current_step: step-1
created_at: '2024-01-15T09:00:00Z'
updated_at: '2024-01-15T10:30:00Z'

steps:
  step-1:
    status: completed
    started_at: '2024-01-15T09:00:00Z'
    completed_at: '2024-01-15T09:30:00Z'
  step-2:
    status: in_progress
    started_at: '2024-01-15T09:35:00Z'
  step-3:
    status: pending
  step-4:
    status: pending
  step-5:
    status: pending
```

### 상태 값

| 상태          | 의미               |
| ------------- | ------------------ |
| `pending`     | 아직 시작하지 않음 |
| `in_progress` | 현재 진행 중       |
| `completed`   | 완료됨             |

### 상태 전이

```
pending → in_progress → completed
```

- 역방향 전이 금지 (롤백 시 전체 Task 재시작)

---

## 5. 사용자 상호작용 규칙

### Step 시작 시 안내

```
📍 현재 상태: Step N (상태)
📋 입력 파일: <파일 목록>
🎯 목표: <Step 목표>

Step N을 시작하시겠습니까?
```

### Step 완료 시 안내

```
✅ Step N 완료
📄 출력 파일: <파일 경로>
📝 Git 커밋: <커밋 해시>

▶️ 다음 Step: Step N+1
계속 진행하시겠습니까?
```

### 오류/예외 발생 시

```
⚠️ 문제 발생: <문제 설명>

선택지:
1. 재시도
2. 이전 Step으로 돌아가기
3. 사용자 개입 요청

어떻게 진행하시겠습니까?
```

---

## 6. 예외 처리 규칙

### 허용되는 예외

- **사용자 명시적 승인** 시 Step 건너뛰기
- **긴급 상황** 시 간소화된 출력 허용
- **반복 작업** 시 이전 출력 재사용

### 금지되는 행위

- 사용자 확인 없이 Step 건너뛰기
- 입력 파일 없이 Step 진행
- Git 커밋 없이 다음 Step 진행
- 이전 Step 출력 파일 임의 수정

---

## 7. 체크리스트

### Step 시작 전

- [ ] status.yaml에서 현재 상태 확인
- [ ] 이전 Step이 completed 상태인지 확인
- [ ] 입력 파일이 모두 존재하는지 확인
- [ ] 입력 파일 내용을 완전히 읽었는지 확인
- [ ] 사용자에게 시작 안내 완료

### Step 진행 중

- [ ] status.yaml을 in_progress로 업데이트
- [ ] 템플릿 형식을 준수하여 출력 작성
- [ ] 중요 결정 시 사용자 확인

### Step 완료 시

- [ ] 출력 파일이 템플릿 형식을 준수하는지 확인
- [ ] status.yaml을 completed로 업데이트
- [ ] Git 커밋 생성 (`[TASK_ID] Step N: 설명`)
- [ ] 사용자에게 완료 안내
