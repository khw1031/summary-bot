# [NUM]-TASK: [작업 제목]

> [이 태스크의 목적을 한 줄로 설명]

## 메타데이터

| 항목            | 값                                      |
| --------------- | --------------------------------------- |
| **Step 2 파일** | [Step 2 5.1에서 할당된 파일]            |
| **관련 TS**     | [Step 2 4.3에서 할당된 테스트 시나리오] |
| **우선순위**    | P0 / P1 / P2                            |
| **의존성**      | [선행 태스크 번호] (없으면 "None")      |
| **차단**        | [이 태스크 완료 후 시작 가능한 태스크]  |

## TDD 요구사항

> **중요**: 구현 전에 테스트를 먼저 작성하세요.

### 관련 테스트 시나리오 (Step 1 8번에서)

| TS ID  | 시나리오     | Given       | When   | Then        |
| ------ | ------------ | ----------- | ------ | ----------- |
| TS-xxx | [시나리오명] | [전제 조건] | [동작] | [기대 결과] |
| TS-xxx | [시나리오명] | [전제 조건] | [동작] | [기대 결과] |

### 테스트 파일

```typescript
// [테스트 파일 경로]

describe('[기능명]', () => {
  // TS-xxx: [시나리오명]
  it('[테스트 설명]', () => {
    // Given
    // When
    // Then
  });

  // TS-xxx: [시나리오명]
  it('[테스트 설명]', () => {
    // Given
    // When
    // Then
  });
});
```

## 목표

이 태스크를 완료하면:

- [ ] [달성할 결과 1]
- [ ] [달성할 결과 2]
- [ ] 관련 TS-xxx 테스트 모두 통과

## 범위

### 포함 사항

- [구현할 기능/파일 1]
- [구현할 기능/파일 2]

### 제외 사항

- [다른 태스크에서 처리할 항목]

## 구현 가이드

### 1. 파일 생성/수정

#### 생성할 파일

```
[파일 경로]
```

#### 수정할 파일

```
[파일 경로] - [수정 내용]
```

### 2. 코드 패턴 참조 (Step 2 6.1에서)

```typescript
// 참조: [Step 2에서 정의한 패턴 경로]
// 이 패턴을 따라 구현하세요
```

### 3. 구현 상세

#### 인터페이스/타입

```typescript
// 주요 인터페이스/타입 정의
interface [InterfaceName] {
  // ...
}
```

#### 핵심 로직

```typescript
// 주요 함수 시그니처
function [functionName]([params]): [ReturnType] {
  // 구현 방향 설명
}
```

## 체크리스트

### 구현 전

- [ ] TASK_MASTER.md의 INSTRUCTION 읽기
- [ ] 의존성 태스크 완료 확인
- [ ] 관련 TS-xxx 시나리오 이해

### TDD 사이클

- [ ] 테스트 코드 먼저 작성
- [ ] 테스트 실패 확인 (Red)
- [ ] 구현 코드 작성
- [ ] 테스트 통과 확인 (Green)
- [ ] 리팩토링 (Refactor)

### 구현 후

- [ ] 모든 관련 TS-xxx 테스트 통과
- [ ] 코드 패턴 가이드 준수 (Step 2 6.1)
- [ ] 금지 사항 위반 없음 (Step 2 6.2)
- [ ] Git 커밋 완료
- [ ] TASK_MASTER.md 진행 상황 업데이트

## Worker 컨텍스트

> 이 섹션은 Step 4에서 Team Worker에게 전달되는 핵심 컨텍스트입니다.
> Coordinator가 TaskCreate 시 이 내용을 description에 인라인합니다.

### 프로젝트 규칙

[TASK_MASTER의 INSTRUCTION에서 발췌 — 코드 패턴 가이드, 금지 사항, 완료 조건]

### 이 태스크의 핵심

- **파일**: {생성/수정할 파일 경로}
- **의존**: {선행 태스크 번호} (없으면 "None")
- **TDD**: {관련 TS-xxx 목록}

---

## 통합 포인트

### 이 태스크의 출력

```typescript
// 다른 태스크가 사용할 수 있는 주요 export
export { [exports] }
```

### 다른 태스크로부터의 입력

```typescript
// 의존하는 다른 태스크의 export
import { [imports] } from '[path]'
```

## 완료 처리

### 1. 테스트 확인

```bash
# 관련 테스트 실행
npm test -- --grep "[테스트 패턴]"

# 모든 테스트 통과 확인
```

### 2. Git 커밋

```bash
git add [변경된 파일들]
git commit -m "feat(<TASK_ID>): [요약] - task-[NUM]"
```

### 3. TASK_MASTER.md 업데이트

진행 상황 표에서 이 태스크 상태 변경:

- `pending` → `completed`
- 테스트 결과: `✅ Pass` 또는 `❌ Fail`
- 완료일 기록

### 4. 다음 태스크 확인

의존성 그래프에서 이 태스크가 차단하던 태스크 확인 후 진행.
